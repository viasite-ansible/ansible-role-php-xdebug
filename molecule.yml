---
dependency:
  name: galaxy
  requirements_file: tests/requirements.yml

driver:
  name: docker
docker:
  containers:
    - name: ansible-role-php-xdebug-1604
      image: ubuntu
      image_version: 16.04

ansible:
  playbook: tests/playbook.yml
